apiVersion: apps/v1
kind: Deployment
metadata:
  name: uad-prod-deployment
  labels:
    app: uad
    environment: prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: uad
      environment: prod
  template:
    metadata:
      labels:
        app: uad
        environment: prod
    spec:
      containers:
      - name: uad-container
        image: uad:v1
        ports:
        - containerPort: 80
